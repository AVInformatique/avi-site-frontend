import { Fragment } from 'react';
import Event from '/src/components/Event/events';

const Events = () => {
    return (
        <Fragment>
            <Event />
        </Fragment>
    );
};

export default Events;
